# Common Components Directory

This directory contains reusable, presentational UI components that are shared across different domains and features.

## Guidelines
- Components here should be highly generic, atomic, and stateless where possible (e.g., standard custom Loader, custom Toast layouts, specific wrappers).
- Avoid putting feature-specific logic inside these components.
- Rely on props to customize behavior and design details.
