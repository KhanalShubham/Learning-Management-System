# Hooks Directory

This folder contains global reusable custom React hooks.
Examples:
- `useDebounce.ts`
- `useLocalStorage.ts`
- `useMediaQuery.ts`

## Guidelines
- Custom hooks here must be feature-agnostic.
- Feature-specific hooks should reside in their corresponding `features/` subdirectories.
