# Types Directory

This directory contains global TypeScript type and interface declarations.

## Guidelines
- Files inside this folder should use the extension `.ts` or `.d.ts`.
- Declare models and common payload models (e.g. paginated list response) here.
- Domain-specific types should go to `features/<feature-name>/types/`.
