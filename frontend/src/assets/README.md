# Assets Directory

This folder contains static assets for the application, such as:
- Images (PNG, JPG, SVG, WebP)
- Global fonts
- Icons
- Standard illustrations

## Guidelines
- Always optimize images before placing them here.
- SVGs should be clean and can be imported as React components or as raw static URLs depending on optimization needs.
