# Prisma Directory

This directory initializes the database client singleton, connection lifecycle logic, and registers logging parameters for database events.

## Key Files
- `client.ts` - Singleton instance export of PrismaClient.
