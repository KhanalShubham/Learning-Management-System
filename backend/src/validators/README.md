# Validators Directory

This directory houses Zod schema validation blocks that guard payload structures.

## Guidelines
- Schemas here must match request bodies, query parameters, or route parameters.
- Expose helper validators to be parsed by validation middlewares.
